<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- BLE (the Dashboard++ GATT central). 12+ uses BLUETOOTH_SCAN/CONNECT;
         ≤31 uses the legacy BLUETOOTH. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- 8-11: the BLE scan needs location (12+ uses BLUETOOTH_SCAN). -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <!-- 14+: the Wi-Fi (NSD) device discovery; never used for location.
         Requested at runtime (MainActivity) like the BLE/POST_NOTIFICATIONS
         grants. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <!-- The NotificationListenerService (the bar's source). 34+ needs
         POST_NOTIFICATIONS; ACCESS_NOTIFICATIONS is the legacy (≤33) gate. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATIONS" />

    <application
        android:name=".DashboardApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Dashboardpp"
        android:allowBackup="false"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|uiMode|keyboard|keyboardHidden|navigation|density|smallestScreenSize|layoutDirection|locale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The §6.1 NotificationListenerService — the bar's source. The
             user grants it from Settings → Apps → Notification access;
             NotifOutbox then pushes the (deduped) alerts / media / nav over
             BLE to the ESP's persistent bar. -->
        <service
            android:name=".notify.NotificationReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.ACTION_NOTIFICATION_LISTENER" />
            </intent-filter>
        </service>
    </application>
</manifest>
