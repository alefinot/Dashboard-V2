<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ESP32 firmware serves over plain HTTP on the local network -->
    <network-security-config>
        <domain-config cleartextPermitted="true">
            <domain includeSubdomains="true">*.local</domain>
        </domain-config>
    </network-security-config>
</resources>
